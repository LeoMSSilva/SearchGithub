import React from 'react';
import Profile from '../components/profile';
import { TouchableOpacity } from 'react-native-gesture-handler';
import { Container } from '../styles/styleSearch';

// @ts-ignore
export default function ShortProfile({ route, navigation, user }) {

	return (
		<Container>
		<TouchableOpacity
		
		>Consultados</TouchableOpacity>
		</Container>
	);
}
